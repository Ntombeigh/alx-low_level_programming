#include <stdio.h>

/**
 * main - Entry - prints the lowercase alphabet in reverse,
 * followed by a new line
 * Return: Always 0 
 */
int main(void)
{
	char ch;

	char for (ch = 'z'; ch >= 'a'; ch--)
	{
		putchar(ch);
	}
	putchar('\n');
	return (0);
}
